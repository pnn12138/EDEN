# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/playwright-report/
/test-results/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts


# codegraph
.codegraph

# local Codex test artifacts
.codex-*.png
.codex-*.log

# local runtime artifacts
server_*.log
server_*_err.txt
server_*_out.txt
server_*_pid.txt

# local dev output
build_output.txt
fake_provider_err.txt
fake_provider_out.txt

# local screenshots
*.jpg
*.png
# 但保留游戏素材图片（成就图标、场景图等），避免部署 404
!public/assets/**/*.png

# Tencent Cloud EdgeOne
.env
.edgeone/*
.tef_dist/*

# 本地验证临时日志/产物
smoke_*.log
smoke_*.txt
vsmoke_*.txt
vsmoke_*.log
srv_*.log
srv_h2*.log
smoke_verify_*.log
.pptx_build/
  
# CodeBuddy .superpowers
.superpowers/

# 独立图生视频测试产物（不提交）
scripts/.i2v-output/

# CodeBuddy 本地规划产物（不提交）
.codebuddy/

# 本地验证/构建产物（不提交）
outputs/

# 本地调试/验证临时文件（不提交）
$null
_dbg.txt
ang_out.txt
angel_log.txt
att_out.txt
attn_log.txt
build_out.txt
c1.txt
c2.txt
c3.txt
c3_log.txt
diff_stat.txt
east_test_out.txt
puz_out.txt
puzzle_log.txt
server_err.txt
server_out.txt
tsc_2r.txt
tsc_t3.txt
typecheck_out.txt
tmp-ending-server.*
tmp-fix-server*.err
tmp-fix-server*.log

# 本地辅助脚本（不提交）
kill_port_3019.ps1
run_smoke.ps1

# agent 工作文档（不提交到公开仓库：Codex/Claude Code/CodeBuddy 内部快照与临时分析）
# 这些文件供本地 Agent 运行/复验使用，不属于游戏源码与交付物，避免污染主开发证据链与公开仓库。
docs/
tmp/

# 与游戏项目无关的工具/IDE 本地配置（不提交、不打包进交付物）
# 注意：根目录 .cnb.yml 与 cnb.config.js 是真实部署配置，保留；此处仅忽略 .cnb/ 本地设置目录。
.claude/
.agents/
.obsidian/
.cnb/

# 项目交付压缩包（本地产物，不提交到仓库）
eden-deliverable.zip

# ============================================================
# 部署上传排除（EdgeOne Pages / edgeone CLI 打包时忽略）
# ------------------------------------------------------------
# 以下目录/文件属于「仓库交付物、评审资料、测试脚本、Agent 工作文档」，
# 游戏运行时完全不依赖。仅从部署上传包中排除以减小构建上下文与上传体积；
# 它们仍保留在 git 仓库中（供赛事评审与提交材料使用）——gitignore 不会删除
# 仓库/本地已有文件，仅阻止其被纳入「部署上传包」。
# 注意：若走「控制台 Git 导入」部署，平台 clone 仍含这些文件（不参与打包、不进
#       .next，仅占用构建上下文）；若要彻底不进构建上下文，请使用 edgeone CLI
#       部署（其打包遵循本 ignore 列表），或确认控制台是否支持忽略目录设置。
# ============================================================
doc/
design/
scripts/
tests/

# Agent / 项目元数据（非运行时）
AGENTS.md
README.md

# 本地资源压缩预览/备份（不提交）
public/assets/.optimized/
public/assets/_originals/

