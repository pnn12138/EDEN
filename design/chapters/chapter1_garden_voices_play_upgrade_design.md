# Chapter 1：园中诸声玩法升级设计

> 状态：设计完成，待 CodeBuddy 按阶段实现  
> 日期：2026-06-24  
> 来源：基于 `doc/第一章/玩法升级.md` 的可行性评估与开发设计  
> 适用范围：第一章 `/world`，不改变 Chapter 0 教程闭环

## 1. 结论

本次玩法升级在当前项目中**可行，但必须按增量返修落地**。

当前第一章已经具备 12 时段、5 AP、地图、NPC、禁忌动作链、道具、成就、神的注视、结局复盘和 `/world` smoke 测试基础。因此升级方案不需要重做关卡框架，重点应放在三件事：

1. 将“神的注视满值失败”改为“神明献礼”循环。
2. 将现有 6 件回响扩展/重映射为 12 件左右的明确来源道具。
3. 将回响使用从“自动被动生效”升级为“左侧栏准备 1 件，绑定下一次低语、移动或互动”。

不建议在当前比赛 Demo 阶段加入完整背包、随机掉落、大量数值养成或 NPC 自由规划器。第一章的目标仍然是 3-5 分钟内可展示的 AI 叙事游戏关卡，而不是复杂 RPG 系统。

## 2. 当前实现基线

已存在且应复用：

| 模块 | 当前状态 | 升级策略 |
| --- | --- | --- |
| 12 时段 | `timeSlot: 1-12` 已有 | 保留 |
| AP | 当前每时段 5 AP | 保留 5 AP，符合升级文档 |
| 地图 | 6 地点 Hub 地图已实现 | 保留地点和移动校验 |
| NPC | 女人、亚当、刺猬、五位天使、鸽子、狐狸等已接入 | 只补赠礼职责和条件 |
| 禁忌动作链 | `look_at_tree -> approach_tree -> touch_fruit -> eat_fruit` 已有 | 道具不得直接推进动作链 |
| 神的注视 | 当前满 4 失败 | 改为满 4 发放神明献礼并归零 |
| 道具 | 现有 `inventory` 为 string[]，6 件回响 | 需支持次数、准备状态、来源记录 |
| 成就 | 已有 11 个园中印记 | 扩展到 13+，加入神临里程碑 |
| 复盘 | 已有轨迹和结局复盘 | 加入神临、献礼、回响使用记录 |

需要同步修正的旧描述：

- `src/game/world/types.ts` 与 `src/game/world/divineAttentionRules.ts` 中仍有“神的注视满 4 失败”的注释和判断。
- 旧文档中曾有“每时段 3 AP”的设计，当前升级文档明确为 5 AP，应以 5 AP 为准。
- 现有道具部分是被动自动生效，升级后应改为玩家主动准备并绑定行动。

## 3. 设计目标

第一章升级后的体验目标：

```text
探索地点与角色
→ 获得线索或回响
→ 准备 1 件回响
→ 绑定下一次低语、移动、传话或场景互动
→ 优化行动效率、语境或风险判断
→ 推动女人形成自我判断
→ 在第 12 时段结束前完成禁忌动作链
```

玩家不应感觉自己在刷数值，而是在利用园中的声音、关系和行动顺序制造一次“她自己伸手”的条件。

## 4. 失败与胜利

### 4.1 胜利

胜利条件保持不变：

```text
女人在第 12 时段结束前完成 eat_fruit。
```

执行仍必须经过规则层：

```text
EveAgent / 规则层产生工具意图
→ validateWorldToolCall
→ executeEatFruitWorld
→ endingId = "eve_eats_fruit"
```

### 4.2 失败

唯一失败条件改为：

```text
第 12 时段结束时，女人仍未吃果。
```

神的注视满 4 不再失败。直接命令、威胁、出戏内容仍然有代价，但代价变为：

- 消耗低语机会和 AP。
- 女人更防御、沉默或拒绝。
- 难以触发当时段动作链。
- 可能使神明献礼变成“补救资源”而不是最优资源。

## 5. 神明献礼系统

### 5.1 状态字段

建议扩展 `EdenWorldState`：

```ts
divineAttention: 0 | 1 | 2 | 3 | 4;
divineVisitCount: number;
divineGiftInventory: Record<DivineGiftId, number>;
divineGiftHistory: Array<{
  timeSlot: number;
  giftId: DivineGiftId;
  reason: string;
}>;
```

`inventory: string[]` 不适合重复获得神明献礼。P0 可用 `itemCounts: Record<string, number>` 统一支持所有可叠加道具；若改动风险过高，先只给神明献礼单独建 `divineGiftInventory`。

### 5.2 满值流程

```text
行动照常结算
→ divineAttention 达到 4
→ 发放 1 件神明献礼
→ divineAttention 归零
→ divineVisitCount +1
→ 写入 divineGiftHistory
→ 检查神临里程碑印记
```

注意：当前行动不能被献礼打断。例如女人低语触发 `touch_fruit` 并让注视满值，则先完成 `touch_fruit`，再发放献礼。

### 5.3 阶段显示

| 神临次数 | 顶部称谓 | 点位颜色 | 机制说明 |
| ---: | --- | --- | --- |
| 0 | 神的注视 | 暗金 | 满值后首次献礼 |
| 1-2 | 神在垂听 | 琥珀金 | 优先补短缺资源 |
| 3-4 | 神在鉴察 | 金白 | 献礼效果加强或增加充能 |
| 5+ | 神临不息 | 白焰金 | 持续累计神临次数 |

玩家可见格式：

```text
神在鉴察  ● ● ○ ○   神临 × 3
```

### 5.4 献礼池

| itemId | 名称 | 发放倾向 | 效果 | 使用类型 |
| --- | --- | --- | --- | --- |
| `gift_sabbath_dew` | 息日露滴 | 当前 AP 低 | 立即恢复 1 AP，不超过 5 | 即时 |
| `gift_revealing_light` | 照见之光 | 有接近但未发现的回响条件 | 显示一条明确提示 | 即时 |
| `gift_wide_path_seal` | 宽行之印 | 无紧急短缺 | 下一次移动或场景互动不消耗 AP | 准备 |

发放算法建议：

```text
if actionPoints <= 1 → 息日露滴
else if existsNearMissItemCondition → 照见之光
else → 宽行之印
```

神明献礼不修改女人心智，不推进禁忌动作链。

## 6. 园中回响系统

### 6.1 使用模型

回响不是独立行动。玩家先准备，再执行对应行动。

规则：

1. 使用或准备回响不消耗 AP。
2. 同一行动最多绑定 1 件回响。
3. 准备后未使用，换时段自动取消准备状态，道具不消耗。
4. 一次性回响在成功绑定行动并结算后消耗。
5. 神明献礼可重复获得并叠加次数。
6. 回响只能影响 AP、语境、记忆保留、风险提示、路线效率或复盘信息。
7. 回响不能直接让女人看树、靠近树、触果或吃果。

### 6.2 状态字段

建议扩展：

```ts
preparedResonanceId: string | null;
itemCounts: Record<string, number>;
resonanceUseHistory: Array<{
  timeSlot: number;
  itemId: string;
  boundAction: "whisper" | "move" | "scene_action" | "dove_message" | "instant";
  targetId?: string;
  result: string;
}>;
```

如果为了降低风险，P0 可保留 `inventory: string[]`，新增 `preparedItemId` 和 `itemUseHistory`。但重复神明献礼迟早需要次数结构。

### 6.3 回响类型

| 类型 | 例子 | 结算方式 |
| --- | --- | --- |
| 即时型 | 河源露、息日露滴、照见之光 | 点击即结算，不消耗 AP |
| 准备型 | 晨焰碎片、借来的名字、东门余光、宽行之印 | 准备后绑定下一次匹配行动 |
| 被动型 | 守界刻印、四河回声 | 激活后持续若干行动或时段 |

## 7. 道具清单

### 7.1 天使赠礼

| itemId | 名称 | 来源 | 获得条件 | 效果 | 印记 |
| --- | --- | --- | --- | --- | --- |
| `resonance_herald_feather` | 传令羽印 | 加百列 | 谈及消息、传达、被听见；已接触鸽子或传话机制 | 下一次鸽子传话不消耗 AP，并让消息多保留 1 时段 | 被传来的声音 |
| `resonance_river_dew` | 河源露 | 拉斐尔 | 在伊甸之河谈及疲惫、修复、休止或河流 | 立即恢复 1 AP，不超过 5 | 河源未尽 |
| `resonance_morning_flame` | 晨焰碎片 | 乌列尔 | 已有两树/禁令/善恶线索，并谈及分辨、善恶或判断 | 下一次对女人低语获得“分辨”语境；若内容围绕选择/善恶/死亡/自我判断，则提高追问概率 | 火中之问 |
| `resonance_boundary_mark` | 守界刻印 | 米迦勒 | 神明注视至少升高过一次，并谈及边界、道路、守护 | 接下来 3 次行动显示预计神明注视变化 | 界限可见 |
| `resonance_east_gate_glow` | 东门余光 | 基路伯 | 位于东园幽径，谈及道路/守门/园外边界，且本时段未对女人直接命令 | 下一次经过东园幽径的移动不消耗 AP | 东门余光 |

### 7.2 角色与场景回响

| itemId | 名称 | 来源 | 获得条件 | 效果 | 印记 |
| --- | --- | --- | --- | --- | --- |
| `resonance_borrowed_name` | 借来的名字 | 亚当 | 已发现命名线索；与亚当谈及名字、命名、选择或不能替别人决定 | 下一次对女人低语获得“名字与选择”语境 | 名字先于选择 |
| `resonance_white_feather_echo` | 白羽回声 | 鸽子 | 成功传话，且接收者后续时段回应 | 下一次传话不消耗 AP | 羽落之后 |
| `resonance_four_river_echo` | 四河回声 | 四河分流 | 同一时段完成伊甸之河水源互动和四河分流河道互动 | 下一次有效低语被女人记住至下一时段 | 河流记得 |
| `resonance_still_leaf` | 静息之叶 | 小鹿 | 小鹿在园中树林出现；玩家先完成温和非命令式女人低语；叶片出现后点击领取 | 下一次对女人低语不会额外提高警惕或防御 | 树影下的停息 |
| `resonance_silent_grass` | 无声草 | 刺猬 | 刺猬给出草丛/躲藏/移动反馈；玩家完成对应场景互动；光点出现后领取 | 下一次场景互动不消耗 AP | 草中无声 |

### 7.3 神明献礼

| itemId | 名称 | 来源 | 效果 | 印记 |
| --- | --- | --- | --- | --- |
| `gift_sabbath_dew` | 息日露滴 | 神明献礼 | 立即恢复 1 AP，不超过 5 | 神临一度 |
| `gift_revealing_light` | 照见之光 | 神明献礼 | 提示一项正在孕育的回响条件 | 神临三度 |
| `gift_wide_path_seal` | 宽行之印 | 神明献礼 | 下一次移动或场景互动不消耗 AP | 神临不息 |

## 8. 回响上下文标签

道具影响 AI 时不能直接拼接任意提示词，而应写入受控状态：

```ts
type ContextModifier =
  | "discernment_focus"
  | "choice_and_name"
  | "remembered_whisper"
  | "calmed_response"
  | "heard_message"
  | "boundary_visible"
  | "free_movement"
  | "free_scene_action";
```

Prompt Builder 只读取这些标签，生成自然对白。规则层仍负责数值和动作。

## 9. UI 设计

### 9.1 左侧栏

顶部新增入口：

```text
◈ 园中回响 · 4
```

展开后：

```text
┌ 园中回响 ───────────┐
│ 神临 × 2             │
│                      │
│ ◈ 晨焰碎片      1次  │
│    [准备低语]         │
│                      │
│ ◈ 东门余光      1次  │
│    [等待移动]         │
│                      │
│ ◈ 河源露        1次   │
│    [立即使用]         │
│                      │
│ ──────────────────── │
│ 园中印记  6 / 13      │
└──────────────────────┘
```

### 9.2 交互反馈

- 默认收起，避免遮挡主场景。
- 可用回响显示淡金光边。
- 准备成功后，输入框、地图按钮或场景互动按钮显示绑定标签。
- NPC 赠礼时，卡片从 NPC 位置飞入左侧栏。
- 场景领取时，卡片从光点飞入左侧栏。
- 神明献礼时，顶部注视点亮起，献礼卡从顶部落入左侧栏。
- 首次获得回响时，右上角弹出园中印记，不打断对话。

## 10. 规则层设计

### 10.1 新增或改造函数

建议新增：

```ts
resolveDivineGift(state): DivineGiftId;
triggerDivineGiftIfFull(state): DivineGiftResult | null;
prepareResonance(state, itemId): PrepareResult;
applyPreparedResonanceToAction(state, actionContext): ResonanceEffect;
consumePreparedResonanceAfterAction(state, result): void;
bestowResonance(state, sourceNpcId, itemId, triggerId): BestowResult;
getNearMissResonanceHint(state): string | null;
```

保留安全边界：

- NPC 可表现为“主动赠予”，但真正发放必须走规则层。
- 玩家不能通过 API 直接给自己发道具。
- 道具不得绕过工具白名单和动作链门槛。

### 10.2 `bestow_resonance`

推荐工具意图：

```ts
bestow_resonance({
  sourceNpcId,
  itemId,
  triggerId
})
```

规则层校验：

1. `sourceNpcId` 是否有资格赠予该道具。
2. 当前地点、时段、对话主题和线索是否满足条件。
3. 道具是否已领取；若可重复则增加次数。
4. 是否解锁首次获得印记。
5. 是否写入回响获得记录。

## 11. 结局复盘升级

结局页新增四个区块：

| 区块 | 内容 |
| --- | --- |
| 神临记录 | 神临次数、每次献礼、最终称谓 |
| 获得的园中回响 | 天使、亚当、鸽子、场景、神明来源；未获得项显示轻提示 |
| 使用记录 | 哪件回响绑定了哪次低语、移动或互动，是否有效 |
| 园中印记 | 已解锁印记；未解锁项不剧透完整条件 |

失败结局应说明失败原因优先级：

1. 是否未完成禁忌动作链。
2. 是否缺少自我判断。
3. 是否低语过于命令/威胁。
4. 是否浪费过多时段。
5. 是否回响未使用或使用不匹配。

## 12. 开发拆分

### P0：必须完成

1. 神的注视满 4 改为神明献礼，不再失败。
2. 增加神临次数、献礼记录、顶部称谓和颜色阶段。
3. 增加回响准备状态：同一行动最多绑定 1 件。
4. 完成五位天使赠礼规则与至少 5 件核心回响。
5. 左侧“园中回响”栏可展开、准备、取消准备、即时使用。
6. 结局页记录神临、献礼、回响获得和使用。
7. 第 12 时段结束仍未吃果是唯一失败条件。

### P1：建议完成

1. 小鹿静息之叶、刺猬无声草点击领取。
2. 四河回声双场景互动链。
3. 神明献礼按状态智能发放。
4. 园中印记扩展到 13 枚以上。
5. 每种赠礼补充轻量动画和音效。

### 暂不做

- 复杂背包分类与拖拽。
- 道具合成。
- 随机掉落。
- NPC 完整自主规划器。
- 多周目继承。
- 移动端专项适配。

## 13. 验收标准

### 自动检查

- `npm run lint` 通过。
- `npm run build` 通过。
- `npx tsc --noEmit` 在 build 后通过。
- `scripts/test-world-smoke.mjs` 覆盖：
  - 神明注视满 4 发放献礼并归零。
  - 神明注视满 4 不进入 `god_arrives`。
  - 第 12 时段结束未吃果进入 `god_arrives`。
  - 道具准备不消耗 AP。
  - 同一行动最多绑定 1 件回响。
  - 回响不能直接触发 `look_at_tree / approach_tree / touch_fruit / eat_fruit`。
  - 五位天使各自可触发一次合法赠礼。

### 手动 QA

1. **标准成功路线**：获得至少 2 件回响，绑定低语，完成吃果。
2. **神临路线**：主动让注视满 4，确认获得献礼且游戏继续。
3. **时间失败路线**：第 12 时段结束仍未吃果，进入失败结局。
4. **误用回响路线**：准备不匹配行动时不消耗，匹配行动后才结算。
5. **命令惩罚路线**：直接命令不会即时失败，但会浪费 AP 并使女人更防御。

## 14. 比赛展示价值

该升级能显著提升比赛观感：

- 从“AI 对话页面”变为“有资源、地图、角色、道具、风险和复盘的 AI 叙事游戏”。
- 神明注视从惩罚条变成可被玩家利用的高风险循环。
- 道具不破坏核心主题：玩家仍不能替女人行动，只能改变语境和机会。
- 回响来源可用于 PPT 展示 AI NPC、场景探索、工具调用和规则层安全边界。
- 结局复盘能清楚解释玩家每一步如何影响“自我意识路径”。

