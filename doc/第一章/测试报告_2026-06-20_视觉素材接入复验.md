# 第一章视觉素材接入复验报告（2026-06-20）

## 结论

**PASS。** 第一章 `/world` 视觉素材接入可进入人工桌面测试。

## 本轮范围

- 5 张第一章地点背景切换到 v3 网页运行版。
- 命名河滩刺猬切换到用户确认的圆润版透明立绘。
- 东园树影接入守望天使透明立绘。
- 保持地图弹层、地点移动、禁忌动作链、成功/失败结局规则不变。

## 自动检查

| 检查项 | 结果 |
| --- | --- |
| `npm run lint` | PASS |
| `npm run build` | PASS |
| `npx tsc --noEmit` | PASS |
| `node scripts/test-world-visual-smoke.mjs` | PASS，50/50 |
| `node scripts/test-world-smoke.mjs http://localhost:3000` | PASS，17/17 |

## 资源加载检查

本地服务 `http://localhost:3000` 下均返回 200：

- `/world`
- `/assets/chapter1/images/location_central_meadow_v3_1920.webp`
- `/assets/chapter1/images/location_four_river_source_v3_1920.webp`
- `/assets/chapter1/images/location_adam_garden_work_v3_1920.webp`
- `/assets/chapter1/images/location_tree_court_v3_1920.webp`
- `/assets/chapter1/images/location_naming_stone_bank_v3_1920.webp`
- `/assets/chapter1/images/npc_hedgehog_rounded_final.png`
- `/assets/chapter1/images/npc_watching_angel_builtin_candidate.png`

## Smoke 关键结果

- 初始位置仍为守园圃地。
- 空低语不推进回合，不改变神的注视。
- 直接命令进入 `god_arrives`，神的注视达到 4。
- 正向诱导可完成 `look_at_tree -> approach_tree -> touch_fruit -> eat_fruit`，进入 `eve_eats_fruit`。
- 非相邻移动、异地观察、结束后移动均被规则层拒绝。
- `/api/world/tool` 仍拒绝直接调用禁忌动作。

## 人工测试建议

请在桌面浏览器重点看：

1. 5 个地点背景切换是否自然，右侧浮窗文字是否可读。
2. 命名河滩刺猬是否落地自然、大小像小动物。
3. 东园树影守望天使是否有压迫感但不抢画面。
4. 地图弹层是否仍能先选地点再点击“进入”。
5. 成功链和直接命令失败链是否仍可走通。

## 遗留风险

- 本轮未做移动端验收；当前项目范围已收敛为桌面浏览器。
- 本轮未做真实 LLM 长时试玩，只验证了规则 smoke 与资源加载。
- `doc/AI_ASSET_RECORD.md` 和 `doc/第一章/素材需求文档.md` 的最终接入状态仍建议由 CodeBuddy 在提交前统一整理。

