# 第一章 `/world` 对话浮窗属性与蛇 Tab 复验报告

日期：2026-06-21  
执行方：Codex（测试与审查辅助）  
范围：第一章 `/world` 右侧对话浮窗优化复验

## 结论

PASS。

开发端报告中的核心改动已核对通过：

- 「属性」Tab 只展示当前选中 NPC 的属性与简洁介绍。
- 未选择 NPC 时，「属性」Tab 显示“请选择一个低语对象”。
- 「蛇（我）」已作为独立 Tab，不再混在 NPC 属性面板中。
- 「蛇（我）」Tab 展示玩家身份、低语限制、词元消耗与禁忌动作链。
- 对话 Tab 保留推荐低语，推荐内容随当前 NPC 变化，点击只填入输入框。
- 第一章浮窗固定高度，Tab 内容在内部滚动，切换 Tab 不应改变窗口尺寸。

## 源码核对

核对文件：

- `src/app/world/page.tsx`
- `src/app/globals.css`
- `scripts/test-world-visual-smoke.mjs`

关键核对点：

- `PanelTab` 包含 `dialogue | mind | serpent | clues | trace`。
- Tab 文案包含 `["mind", "属性"]` 和 `["serpent", "蛇（我）"]`。
- `buildAttributeProfile(activeNpc, state)` 用于当前 NPC 属性渲染。
- `activeTab === "serpent"` 独立渲染蛇的玩家面板。
- `getRecommendedWhispers(activeNpc)` 负责按 NPC 生成推荐低语。
- `.eden-game--world .eden-world-panel` 固定高度。
- `.eden-game--world .eden-panel-content` 使用内部滚动。

本轮还修正了静态检查中的旧断言：原检查仍写“属性面板包含蛇与词元消耗”，已改为分别检查“蛇是独立 Tab”和“蛇 Tab 包含词元消耗”。

## 自动检查结果

| 检查项 | 结果 |
| --- | --- |
| `node scripts/test-world-visual-smoke.mjs` | PASS，58/58 |
| `npm run lint` | PASS，无 ESLint warning/error |
| `npx tsc --noEmit` | PASS |
| `npm run build` | PASS |
| `node scripts/test-world-smoke.mjs http://localhost:3010` | PASS，17/17 |

## `/world` 运行 Smoke 结果

服务地址：`http://localhost:3010`

通过场景：

1. 空低语不推进：5/5 通过。
2. 直接命令进入 `god_arrives`：2/2 通过。
3. 正向诱导进入 `eve_eats_fruit`：4 步禁忌链完成并吃果成功，3/3 通过。
4. 非相邻移动、异地观察、结束状态工具调用边界：5/5 通过。
5. 禁忌动作链不能通过通用工具接口直接调用：2/2 通过。

合计：17/17 通过。

## 注意事项

- 第一次运行 `/world` smoke 曾因 dev server 冷启动/API 编译超时；重启服务后复跑通过。
- 本轮只验证桌面浏览器范围。移动端仍按当前项目约定退出验收范围。
- 工作区存在大量开发端既有未提交/未跟踪文件，本轮未回退这些内容。

## 复测入口

本地服务已重启，可打开：

`http://localhost:3010/world`
